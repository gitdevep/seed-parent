server:
  port: 9004

chenyh:
  service:
    version: 1.0.0
swagger:
  enable: true

embedded:
  zookeeper:
    port: 2181

dubbo:
  registry:
    address: zookeeper://127.0.0.1:${embedded.zookeeper.port}
  application:
    name: iWorld
